\section{Selected Code Listings and Algorithm Pseudocode}
\label{app:code}

% CUED: do NOT dump the whole codebase. Pick novel snippets only.
% Day 7 (5/31).
%
% Candidate listings (select 2-3):
%   - auto\_detect\_line\_dir spectral-power core (the dual-band
%     cross-validation logic) -- this is the novel bit.
%   - patchwise trigonometric collapse (azimuth averaging in (cos 2A, sin 2A)).
%   - self-contrast null-distribution computation.
%
% Use pseudo-code (algorithm environment) rather than raw Python if listings
% get too long.
